# Constructor backup 2026-04-01

Точка отката для рабочей версии конструктора после фикса resize фигур, tight frame и effect-aware shape preview.

Содержимое:
- `constructor/` — копия `src/components/constructor/`
- `useConstructorState.js` — копия `src/hooks/useConstructorState.js`
- `utils/constructor/` — копия `src/utils/constructor/`

Назначение:
- быстрый ручной откат к стабильной версии конструктора
- сохранение совместимой связки UI + state hook + shape resize/utils
