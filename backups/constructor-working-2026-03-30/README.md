# Constructor backup 2026-03-30

Точка отката для рабочей версии конструктора.

Содержимое:
- `constructor/` — полная копия `src/components/constructor/`
- `useConstructorState.js` — копия `src/hooks/useConstructorState.js`

Назначение:
- быстрый ручной откат конструктора к текущему рабочему состоянию
- сохранение совместимой связки UI + state hook
